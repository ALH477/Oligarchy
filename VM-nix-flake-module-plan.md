cores 0-1 pinned with full IOMMU for the DSP VM using ArchibaldOS. When using a 6 core CPU. just 0 when using a quad core of smaller. with 2-4GB of RAM dedicated to it

the second to last core is used for a headless arch linux VM used as a sandbox for coding agents. with 2-4GB of RAM dedicated to it

the last core is dedicated to a kali linux VM. with 4 GB of RAM dedicated to it
