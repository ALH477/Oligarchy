[package]
name = "forge-tui"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Read-only Ratatui dashboard for oligarchy-forge: session list + live nix build log"

[dependencies]
forge-core = { path = "../forge-core" }
ratatui = "0.29"
crossterm = "0.28"
anyhow = "1"
